TURNO DE REPLICA

Sitio estático generado con Eleventy.

Para instalar las dependencias:
  npm ci

Para generar la versión publicada:
  npm run build

La salida se crea en la carpeta _site. Los contenidos editables del sitio se
encuentran en content/ y las páginas en los archivos HTML de esta carpeta.
